---
interface Props {
  position?: 'top-left' | 'top-right' | 'top-center' | 'page-top-left' | 'page-top-right';
}
const { position = 'top-center' } = Astro.props;
---

<span class="tape" data-pos={position} aria-hidden="true"></span>
